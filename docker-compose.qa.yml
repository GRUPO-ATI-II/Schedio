services:
  backend:
    build:
      target: production
    environment:
      - NODE_ENV=qa

  frontend:
    build:
      target: server
    ports:
      - "4100:80"
    environment:
      - NODE_ENV=qa
